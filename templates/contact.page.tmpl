{{template "base" .}}

{{define "title"}}
    <title>Contact</title>
{{end}}

{{define "content"}}
    <div class="container" style="margin-top: 60px">
        <div class="row">
            <div class="col">
                <h1>This is the contact Page</h1>
            </div>
        </div>
    </div>
{{end}}