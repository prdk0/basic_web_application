run go web without running test files
--------------------------------------
go run $(ls -1 ./cmd/web/*.go | grep -v _test.go)

profile coverage
-------------------------------------
go test -coverprofile=coverage.out && go tool cover -html=coverage.out